using UnityEngine;

public class buy : MonoBehaviour
{
    //1.Selección del elemento con el cursor (es un objeto inscrito a otro script(scriptableObject)
   //2. El jugador clica el botón buy 
   //3. Arrastrar automáticamente el elemento anteriormente seleccionado en el otro inventario

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
